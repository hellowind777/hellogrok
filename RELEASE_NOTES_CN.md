# 发布说明 — v0.1.14

## 受管配置写入统一使用 UTF-8 无 BOM

hellogrok 继续支持读取带或不带字节顺序标记的 UTF-8 `config.toml`，只读检查不会改写文件。代理每次成功应用、恢复或回滚受管设置时，都会通过原子写入把配置保存为 UTF-8 无 BOM。

如果用户在代理运行期间添加 BOM，后续成功恢复会删除 BOM，同时保留并发产生的字段修改。无关编辑让 TOML 暂时无效但文本仍是有效 UTF-8 时，逐行恢复仍可处理受管设置；成功恢复的结果同样使用 UTF-8 无 BOM。

## 恢复配置时对无效编码保持失败关闭

逐行恢复现在只会在配置通过 UTF-8 校验后运行。UTF-8 无效时，错误会显示配置路径，以及第一个无效位置的行号、列号和字节偏移，不会输出配置值。配置字节和恢复状态都会保持不变，以便修正编码后重新恢复。hellogrok 不会猜测或转码 GBK、UTF-16 等其他编码。
