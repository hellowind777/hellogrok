# 发布说明 — v0.1.11

## 托盘退出始终完成

选择“退出”后，hellogrok 会先完整尝试恢复配置并停止代理，随后始终结束进程。清理错误不再让用户被困在托盘程序中。配置无法访问或本地路由无法安全恢复时，恢复事务会保留在磁盘上，后续启动会在加载任何路由前再次尝试。

普通停止代理操作在其他供应商管理工具持有 Grok Build，或仍有不属于原事务结构的本地路由时，继续保持失败关闭。这样既保留日常停止的谨慎行为，也不会再把应用退出变成永久锁定。

## 无效 TOML 不再阻塞受管字段恢复

用户可以在代理运行期间编辑 `config.toml`，包括暂存未完成的值，或写入带异常字节顺序标记的文本。整份文档无法解析时，hellogrok 现在会回退到逐行恢复：独立比较每个受管赋值，恢复仍未被用户改动的临时值，同时原样保留用户拥有的字节。

回退路径仍会确认所有 `127.0.0.1:18787` 渠道路由已清除。若模型被改名或结构发生变化，导致该路由仍存在且无法证明归属，hellogrok 会保留配置与恢复事务，不执行不安全改写。
