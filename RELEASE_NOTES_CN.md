# 发布说明 — v0.1.26

## 响应头超时回到设计好的重试路径

- 此前响应头超时会取消整个重试循环共享的上下文：吸收层的等待立即返回，处理函数没有写出任何响应，客户端收到一个空的 `200` 而不是设计中的可重试 `504`，响应头超时之后的吸收重试成为永远走不到的死路径。
- 现在超时只取消本次尝试的派生上下文。响应头超时会产生可重试的 `504`，吸收层在配置预算内真正执行重试，重试成功后客户端拿到正常响应；健康的长流不再被单次尝试的计时逻辑掐断。
- 新增回归测试覆盖两条路径：可重试 `504` 的写出，以及响应头超时后吸收重试成功。

## 死渠道熔断的探测不会再永久锁死

- 探测若始终没有报告结果——调用方中途断开，或探测的响应头超时——就永远不会走到成功/失败记录，熔断器停留在探测中状态：即使网络恢复，该渠道也会永久快速失败。
- 现在每次探测携带 2 分钟租约。租约到期仍无结果即视为探测丢失，下一个请求会放行一次新的探测。正常报告结果的探测仍按原逻辑自动合闸或重新计时。

## 应用与恢复期间保留并发的 `config.toml` 修改

- 应用和恢复在原子重命名前会重新读取 `config.toml`。若本次操作读取之后有其他程序改动了该文件，写入会在触碰文件前以可重试错误中止：外部修改原样保留，改写状态仍在磁盘上，直接重试即可。
- 原子写入现在还会刷新已重命名文件的目录项，断电不会丢失已经提交的重命名（不支持目录同步的文件系统上尽力而为）。
- 恢复时只会删除文件根表中由 hellogrok 创建的 `subagents.enabled` 点号键。用户写在其他表（例如 `[model.one]`）里的同名键不再被误判为临时投影而删除。

## 恢复记录与派生状态不再因损坏或缺失而卡死

- `config.toml` 已不存在时没有可恢复的内容：`hellogrok restore` 现在会清除过期的恢复记录，而不是让之后每次启动和恢复都在一条永远无法成功的记录上失败。改写状态格式不受支持的错误信息现在会指出应删除的状态文件。
- 损坏的 `capacity_cache.json` 或偏好文件不再阻塞启动，也不再让之后每次读写失败。无法解析的文件会以 `.bad` 后缀保留在原文件旁，应用以空容量缓存或默认偏好继续运行；两者都是纯派生状态，会随实际流量和下一次写入自动重建。

## Windows 控制台与日志窗口的健壮性

- 除“控制台已存在”以外的控制台分配失败现在会作为错误上报；绑定 `CONOUT$` / `CONIN$` 失败时返回错误，不再静默留下未绑定的标准流。
- 窗口仍在创建时重复点击“状态与日志”不会再发起第二次创建；创建失败或超时后，下一次点击可以重试。创建超时的错误信息现在指向实际的日志文件路径。

升级后请重启两个 hellogrok 可执行文件，并新开一轮会话。已存盘的对话气泡不会改写。
