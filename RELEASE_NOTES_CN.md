# 发布说明 — v0.1.13

## UTF-8 BOM 不再阻止启动

hellogrok 现在可以读取带或不带字节顺序标记的 UTF-8 `config.toml`。标准 BOM 不会再让代理在启动前的配置归属检查阶段失败。

普通代理事务会保留文件当前的 BOM 选择。如果用户在代理运行期间添加或删除 BOM，停止时会保留这项修改，只恢复 hellogrok 管理的设置。hellogrok 自己拥有的运行状态、偏好和容量缓存文件继续统一使用 UTF-8 无 BOM。

## 可以直接定位的配置错误

TOML 语法错误现在会显示配置文件路径，以及解析器给出的行号和列号。UTF-8 无效时会报告第一个无效位置，不输出配置值。hellogrok 不会猜测或静默转码 GBK、UTF-16 等其他编码。

## 显式规范化配置编码

新增 `hellogrok normalize-config` 命令：校验整份 TOML 后，以原子方式删除有效的 UTF-8 BOM。配置内容无效、hellogrok 代理仍在运行，或其他供应商管理工具正在接管 Grok Build 时，命令会拒绝执行。普通启动不会静默改变文件编码。
