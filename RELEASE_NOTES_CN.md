# 发布说明 — v0.1.12

## 为每个自定义模型安全触发自动压缩

hellogrok 现在会为每个自定义模型分别计算自动压缩预算。计算时预留模型最大输出额度，再保留 5% 且不低于 8K 的上下文余量，只临时降低触发过晚的阈值，不会提高用户设置的较低值。切换渠道后会重新加载所选模型自己的预算，不再让所有渠道共用一个全局最小值。

这项修正解决了一个关键问题：Grok Build 会直接用完整的 `context_window` 乘以 `auto_compact_threshold_percent`，不会先减去 `max_completion_tokens`。例如，上下文窗口为 1,048,576 token、最大输出为 384,000 token 时，默认的 85% 首选值会被安全限制为 58%。

## 无需模型名称规则的容量学习

显式配置的模型或供应商容量始终优先。容量字段缺失时，hellogrok 可以从实际发出的输出额度、有效上游容量响应头，或唯一且无歧义的结构化上下文错误中学习。可信的上下文窗口会临时写入模型级配置，使 Grok Build 使用相同分母；从请求中观察到的输出值只参与预算，不会变成模型输出上限。

学习记录只使用哈希路由标识，不包含 URL、模型名称或凭据，并在 30 天后过期。如果一个或两个容量值仍未知，hellogrok 会报告该模型仍在学习并保持原阈值，不根据模型名称猜测。

## 运行时事务随时可以退出

运行时容量变化复用现有的可恢复配置事务，并刷新空闲的 Grok 会话。活动会话重试有明确上限，会合并重复请求，也可以取消；停止代理时会先取消并等待两个后台工作器结束，再恢复配置，因此新增的自动处理不会阻止托盘“退出”完成。

Windows 原子更新遇到短暂共享冲突时会进行有界重试。若最终仍无法提交，hellogrok 会保留上一份活动代理配置及其匹配的恢复状态，不会在进程仍运行时把整份代理配置回滚。正常停止仍会恢复原始字节，并保留代理运行期间的用户修改，包括 TOML 暂时无效时保存的修改。
