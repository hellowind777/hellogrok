# 发布说明 — v0.1.28

## 中转线路缺陷不再杀死"重试一次就能好"的轮次

- **包在 HTTP 200 里的失败。** 部分 newapi/sub2api 中转会用成功状态码加错误信封回复失败。此前该正文落入信封校验，以不透明且不可重试的拒绝告终。hellogrok 现在识别这种包裹信封：瞬态语义（限流、过载、超时）进入吸收窗口并在代理内重试；确定性语义（鉴权、账单、无效请求）带着供应商自己的解释透传。自带 error 成员的 Responses 终止响应仍走原生失败路径，真实的 `response.failed` 正文不受影响。
- **Cloudflare 盾质询。** 中转前面的质询页（`cf-mitigated: challenge`，或 Cloudflare 下发的 "Just a moment..." 页面）本质是瞬态故障。它现在会在重试窗口内被吸收；超出窗口的质询以可重试的 `503` 透传，因为裸 `403` 在 Grok Build 中属终止类，会让可自行清除的盾质询直接杀死本轮。不带 Cloudflare 标记的真实源站 `403` 不受影响。
- **非流式响应中的重复 Chat 工具调用 ID。** 流式重组器早已对中转复用的 `tool_call` ID 换 ID 容忍；非流式路径此前却拒绝同一可修复缺陷。两条路径现在都改写为新唯一 ID，保证下一轮工具历史有效，而不是让本轮失败。
- **缺失的 Responses 信封簿记。** 中转在完整终止响应上省略 `id`、`object` 或 `status` 时，现在会合成缺失标记，而不是以 502 拒绝。字段存在但取值错误、`output` 缺失或畸形，仍然按错误拒绝。

所有新增容忍都留在既有安全合同内：修复发生在 Grok Build 严格校验器看到线路之前；拒绝仍一律不可重试，被拒请求不会重新进入重试循环；无需新增配置。每条新路径都有"吸收后恢复"与"透传"两类回归测试覆盖。

升级后请重启两个 hellogrok 可执行文件。
