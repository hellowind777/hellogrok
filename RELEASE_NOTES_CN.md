# 发布说明 — v0.1.17

## 自定义渠道在 `/resume` 后保持原身份

每个代理自定义渠道现在都使用模型表 ID 作为 Grok Build 看到并保存的运行时身份。配置的 `model` 仍是发送给上游的模型，代理停止时会逐字节恢复原始配置。

因此，即使多个自定义渠道和官方模型使用相同的上游模型名，例如 `grok-4.6`，会话仍能通过创建它的渠道恢复。已经只保存共用模型名的历史会话摘要需要手动重新选择一次模型。

## 供应商错误保持清晰可处理

hellogrok 现在会保留上游错误状态和正文，并根据结构化错误代码和消息判断是否重试。鉴权、权限、账单、余额或额度不足、无效请求和无效模型错误不可重试；限流、超时、过载和临时服务错误仍可重试。上游显式返回的 `X-Should-Retry` 始终优先。

兼容中转返回的账单和账号错误因此可以直接进入对话，不再被通用重试掩盖。

## 已停止的会话会收到明确提示

普通停止代理后会保留诊断监听，让旧会话收到结构化、不可重试的 `proxy_stopped` 响应，并提示用户重新选择模型。托盘“退出”在尝试恢复配置后始终会关闭监听并释放本地端口，即使清理需要推迟也不例外。
