# 发布说明 — v0.1.27

## 中转源站 TLS 失败（525/526）不再一次即死

- `Server error (525): Something went wrong on our side.` 是 Grok Build 对所有 5xx 的通用文案：该状态来自中转的 Cloudflare 边缘节点，它与中转自己的源站 TLS 握手失败（525）或源站证书无效（526）。请求根本没有到达中转的应用；Grok Build 把这两个状态归为终止类，因此即使中转几秒后就恢复，本轮也会在首次出现时直接失败。
- hellogrok 现在会在吸收窗口内（默认 90 秒，可按渠道配置 `absorb_retry_max_secs`）重试 525/526。在边缘与源站 TLS 握手阶段失败的请求从未到达源站应用，重放没有副作用。窗口内恢复的源站重启或证书轮换从此对用户不可见；窗口耗尽后按 Grok Build 预期的终止判定透传。
- 透传的重试判定现在与 Grok Build 的边缘客户端策略一致：除源站 TLS 两个状态码外，`429` 和所有 `5xx` 都可重试。此前 hellogrok 会给瞬态 Cloudflare 边缘页（`520`–`524`、`529`、`530`）加盖 `X-Should-Retry: false`，而该请求头会否决 Grok Build 对可自行恢复故障的原生重试——把一次自愈的边缘抖动变成即时失败。
- 回归测试覆盖 525 吸收后恢复、窗口耗尽后的终止透传，以及边缘页的可重试判定。

升级后请重启两个 hellogrok 可执行文件。持续报 525 的渠道存在源站证书或 TLS 终止配置错误，只能由中转运营方修复。
